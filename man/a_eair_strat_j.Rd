% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_eair_strat_j.R
\name{a_eair_strat_j}
\alias{a_eair_strat_j}
\alias{s_eair_strat_levii_j}
\title{Stratified Exposure-Adjusted Incidence Rate (EAIR)}
\usage{
s_eair_strat_levii_j(
  levii,
  df,
  .df_row,
  .alt_df_full,
  .var,
  strata,
  id = "USUBJID",
  fup_var,
  occ_var = NULL,
  occ_dy = NULL,
  num_p_year = 100,
  count_multiple_events = FALSE,
  conf_level = 0.95,
  vs_ref_group = FALSE,
  .in_ref_col,
  cur_col_expr,
  ref_col_expr,
  inriskdiffcol = FALSE
)

a_eair_strat_j(
  df,
  labelstr = NULL,
  .var,
  .df_row,
  .spl_context,
  .alt_df_full = NULL,
  id = "USUBJID",
  strata,
  fup_var,
  occ_var = NULL,
  occ_dy = NULL,
  num_p_year = 100,
  count_multiple_events = FALSE,
  drop_levels = FALSE,
  riskdiff = FALSE,
  ref_path = NULL,
  conf_level = 0.95,
  .stats = "eair_strat",
  .formats = NULL,
  .labels = NULL,
  .indent_mods = NULL,
  na_str = rep("NA", 3),
  row_labels_adj = FALSE,
  riskdiff_setup = c("horizontal", "vertical")
)
}
\arguments{
\item{levii}{(\code{string})\cr the specific level of the variable to calculate EAIR for.}

\item{df}{(\code{data.frame})\cr data set containing all analysis variables.}

\item{.df_row}{(\code{data.frame})\cr data frame across all of the columns for the given row split.}

\item{.alt_df_full}{(\code{dataframe})\cr alternative dataset for calculations.}

\item{.var}{(\code{string})\cr variable name that is passed by \code{rtables}.}

\item{strata}{(\code{string})\cr variable identifying strata (e.g., study identifier).}

\item{id}{(\code{string})\cr subject variable name.}

\item{fup_var}{(\code{string})\cr name of the variable containing the total follow-up duration
for each subject, expressed in \strong{years}.
Used as the at-risk exposure time for subjects who did \strong{not} experience the event.}

\item{occ_var}{(\code{string} or \code{NULL})\cr name of the flag variable identifying the
\strong{first} occurrence of the event within each subject and level, encoded as \code{"Y"}.
Only records where \code{occ_var == "Y"} contribute to the event count (\code{n_event}),
and only one event record per subject is allowed.
\cr If \code{NULL}, event occurrence filtering is skipped and time at risk is taken
from \code{fup_var} in \code{alt_counts_df} for all subjects.}

\item{occ_dy}{(\code{string} or \code{NULL})\cr When \code{occ_var} not \code{NULL}: name of the variable
containing the relative day of the
first event occurrence, expressed in \strong{days} (e.g., \code{ASTDY}).
For subjects with an event (\code{occ_var == "Y"}), this value is converted to years
as \code{occ_dy / 365.25} and used as the at-risk exposure time instead of \code{fup_var}.
Must be on the same time origin as \code{fup_var} (i.e., relative to start of exposure).}

\item{num_p_year}{(\code{numeric})\cr scaling factor for the incidence rate, representing
the number of person-years used as the denominator base.
The EAIR is calculated as \code{num_p_year * n_event / total_person_years}.
Defaults to \code{100}, yielding a rate per 100 person-years.
For example, use \code{1} for a rate per person-year or \code{1000} for a rate per
1000 person-years.}

\item{count_multiple_events}{(\code{logical})\cr if \code{TRUE}, counts the total number of events
per subject rather than treating each subject as contributing 1 to \code{n_event}.
Only considered when \code{occ_var = NULL}.
Defaults to \code{FALSE}.}

\item{conf_level}{(\code{proportion})\cr confidence level of the interval.}

\item{vs_ref_group}{(\code{logical})\cr if TRUE, risk difference (against reference group) calculations will be performed.}

\item{cur_col_expr}{(\code{expression})\cr current column expression taken from \code{rtables} \code{.spl_context}
splitting machinery (\code{cur_col_expr}).}

\item{ref_col_expr}{(\code{expression} or \code{NULL})\cr column expression for the reference group.
Required to be an expression if \code{vs_ref_group} is \code{TRUE}}

\item{inriskdiffcol}{(\code{logical})\cr flag indicating if the function is called within a risk difference column.}

\item{labelstr}{(\code{string})\cr Not intended to be set by user, only present to be able to use function as a \code{cfun}.}

\item{.spl_context}{(\code{data.frame})\cr gives information about ancestor split states.}

\item{drop_levels}{(\code{logical})\cr if TRUE, non-observed levels will not be included.}

\item{riskdiff}{(\code{logical})\cr if TRUE, risk difference calculations will be performed.}

\item{ref_path}{(\code{string})\cr column path specifications for the control group.}

\item{.stats}{(\code{character})\cr statistics to select for the table.\cr}

\item{.formats}{(named 'character' or 'list')\cr formats for the statistics.}

\item{.labels}{(named 'character')\cr labels for the statistics.}

\item{.indent_mods}{(named \code{integer})\cr indent modifiers for the labels.}

\item{na_str}{(\code{string})\cr string used to replace all NA or empty values in the output.}

\item{row_labels_adj}{(\code{logical})\cr If \code{TRUE} and number of requested statistics = 1,
the row label on the table will reflect the level of the incoming variable rather than
the label for the statistic.
\cr Set to default \code{TRUE} for backward-compatibility reasons.}

\item{riskdiff_setup}{(\code{string})\cr Are risk differences presented in horizontal way, or vertical way.
\cr When horizontal, the statistics for \code{eair}, \code{n_eair}, \code{eair_n} will be replaced by
\code{ear_diff_est_ci} in the risk difference columns.
If \code{.stats} include any of difference in eair rates statistics,
these will be removed as \code{ear_diff_est_ci} will be displayed in the risk difference columns}
}
\value{
\itemize{
\item \code{s_eair_strat_levii_j()} returns a list containing the following statistics:
\itemize{
\item \code{n_event_total}: total event count across strata (current arm)
\item \code{person_years_total}: total person-years across strata (current arm)
\item \code{eair_strat}: stratified pooled EAIR per \code{num_p_year} person-years
\item \code{n_eair_strat}: composite of \code{n_event_total} and \code{eair_strat}
\item \code{eair_strat_ci}: Wald CI for \code{eair_strat}
\item \code{eair_strat_est_ci}: composite of \code{eair_strat} and \code{eair_strat_ci}
\item \code{eair_strat_n}: composite of \code{eair_strat} and \code{n_event_total}
\item \code{eair_strat_n_py}: composite of \code{eair_strat}, \code{n_event_total} and \code{person_years_total}
\item \code{eair_strat_diff_est_ci}: stratified rate difference with Wald CI
(only when \code{vs_ref_group = TRUE}, otherwise \code{NULL})
\item \code{eair_strat_diff_est}: estimate for the difference in stratified rate
\item \code{eair_strat_diff_ci}: CI (Wald type) for the difference in stratified rate
}\cr
The list of available statistics (core columns) can also be viewed by
running \code{junco_get_stats("a_eair_strat_j")}.
}

\itemize{
\item \code{a_eair_strat_j()} returns a list of formatted \code{\link[rtables:CellValue]{rtables::CellValue()}} rows.
Within a risk difference column,
the statistics \code{eair_strat} and \code{n_eair_strat} are replaced by \code{eair_strat_diff_est_ci}
(difference in EAIR between current group and reference group,
together with it's confidence interval).
}
}
\description{
Statistical/Analysis function for presenting a stratified exposure-adjusted
incidence rate (EAIR) and its Wald confidence interval across strata
(e.g., study size weighted exposure).

This function is an extension to the unstratified function \code{a_eair100_j()}.
}
\details{
The weighting method uses raw event counts \code{n_ti} in both the rate and variance.
\deqn{\hat r_{ti} = \texttt{num\_p\_year} \times \frac{n_{ti}}{T_{ti}}}

The strata weight is \code{w_i = T_0i + T_1i} (total person-years across
\strong{all} groups in strata \code{i}), and the pooled weighted rate is:
\deqn{\hat r_t = \sum_i \frac{w_i}{W} \hat r_{ti}, \quad W = \sum_i w_i}

The Wald 95\% CI for the pooled rate is:
\deqn{\hat r_t \pm z_{1-\alpha/2} \times \frac{\texttt{num\_p\_year}}{W}
  \sqrt{\sum_i w_i^2 \frac{n_{ti}}{T_{ti}^2}}}.

The weighted rate difference and its Wald CI are:
\deqn{\hat\delta = \sum_i \frac{w_i}{W}(\hat r_{1i} - \hat r_{0i})}
\deqn{\hat\delta \pm z_{1-\alpha/2} \times \frac{\texttt{num\_p\_year}}{W}
  \sqrt{\sum_i w_i^2 \left(\frac{n_{1i}+c}{T_{1i}^2} + \frac{n_{0i}}{T_{0i}^2}\right)}}
}
\section{Functions}{
\itemize{
\item \code{a_eair_strat_j()}: Formatted analysis function for stratified (eg study size-weighted) EAIR, used as \code{afun}
in \code{\link[rtables:analyze]{rtables::analyze()}}.

}}
\examples{
library(tern)
library(dplyr)

set.seed(42)
n <- 120
adsl <- data.frame(
  USUBJID = paste0("S", seq_len(n)),
  ARM = rep(c("Treatment", "Placebo"), each = n / 2),
  STUDY = rep(c("Study1", "Study2", "Study3"), times = n / 3),
  TRTDURY = runif(n, 0.5, 3),
  stringsAsFactors = FALSE
)
adae <- data.frame(
  USUBJID = sample(adsl$USUBJID, 40),
  AEDECOD = "Headache",
  ASTDY = sample(30:500, 40, replace = TRUE),
  AOCCPFL = "Y",
  stringsAsFactors = FALSE
)
aefup <- merge(adae, adsl, by = "USUBJID")

# Standard method (matches rdcomaeinci SAS macro)
lyt <- basic_table() |>
  split_cols_by("ARM") |>
  analyze(
    "AEDECOD",
    afun = a_eair_strat_j,
    extra_args = list(
      strata = "STUDY",
      fup_var = "TRTDURY",
      occ_var = "AOCCPFL",
      occ_dy = "ASTDY",
      .stats = c("eair_strat", "eair_strat_ci")
    )
  )
result <- build_table(lyt, aefup, alt_counts_df = adsl)
result
}
\references{
Scosyrev, E & Pethe, A (2022). Confidence intervals for exposure-adjusted rate
differences in randomized trials.
Pharmaceutical Statistics, 2022;21:103–121.
doi: 10.1002/pst.2155

Crowe, B., Chuang-Stein, C., Lettis, S., & Brueckner, A. (2016).
Reporting adverse drug reactions in product labels.
Therapeutic Innovation & Regulatory Science, 50(4), 455-463.
doi: 10.1177/2168479016628574
}
\seealso{
\code{\link[=a_eair100_j]{a_eair100_j()}}
}
\keyword{internal}
