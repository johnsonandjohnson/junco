% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{extract_vectors}
\alias{extract_vectors}
\title{Extract Vectors for Two-Sample Analysis}
\usage{
extract_vectors(df1, df2, .var, paired = FALSE, paired_by)
}
\arguments{
\item{df1}{(\code{data.frame})\cr First dataset.}

\item{df2}{(\code{data.frame})\cr Second dataset.}

\item{.var}{(\code{character(1)})\cr Name of the numeric variable to extract from
both datasets.}

\item{paired}{(\code{logical(1)})\cr Whether the values in \code{df1[[.var]]} and
\code{df2[[.var]]} should be treated as paired (matched) samples.}

\item{paired_by}{(\code{character})\cr Column name(s) used to match observations
between \code{df1} and \code{df2}. Required only if \code{paired = TRUE}.}
}
\value{
A named \code{list} with:
\describe{
\item{x1}{Numeric vector from \code{df1}.}
\item{x2}{Numeric vector from \code{df2}.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Extracts numeric vectors from two data frames for two-sample statistical
analysis. The function supports both unpaired and paired settings.

For unpaired data, values are extracted directly from each dataset with
missing values removed independently.

For paired data, observations are first matched using the key variable(s)
specified in \code{paired_by}, and only complete pairs are retained.
}
\details{
The function performs the following steps depending on the \code{paired} flag:

\strong{Unpaired case:}
\enumerate{
\item Extract \code{.var} from each dataset.
\item Remove \code{NA} values independently from each vector.
}

\strong{Paired case:}
\enumerate{
\item Checks that \code{paired_by} uniquely identifies rows in both datasets.
\item Merges \code{df1} and \code{df2} by \code{paired_by}.
\item Removes rows with missing values in either matched variable.
\item Extracts aligned numeric vectors.
}

Missing values (\code{NA}, \code{NaN}) in pairing variables are treated as
incomparable and excluded during matching.

This function is intended for internal use in two-sample statistical
procedures such as paired and unpaired t-tests.
}
\examples{
df1 <- data.frame(id = c("A", "B", "C", "D"), value = 1:4)
df2 <- data.frame(id = c("A", "C", "D", "E", "F"), value = c(11, 13:14, NA, 16))
df1
df2

# Unpaired
\dontrun{
extract_vectors(df1, df2, "value")
}

# Paired
\dontrun{
extract_vectors(df1, df2, "value", paired = TRUE, paired_by = "id")
}
}
\keyword{internal}
