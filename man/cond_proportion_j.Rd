% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s_cond_proportion_j.R
\name{cond_proportion_j}
\alias{cond_proportion_j}
\alias{s_cond_proportion_j}
\alias{d_cond_proportion_j}
\alias{a_cond_proportion_j}
\title{Conditional proportion with adaptive CI (exact vs. Wald)}
\usage{
s_cond_proportion_j(
  df,
  .var,
  conf_level = 0.95,
  long = FALSE,
  na.rm = TRUE,
  num_limit = 0,
  denom_limit = 10,
  denom = c("n", "N_col", "N_row"),
  .N_row,
  .N_col
)

d_cond_proportion_j(conf_level, long = FALSE, num_limit, denom_limit)

a_cond_proportion_j(
  df,
  .var,
  ...,
  .stats = NULL,
  .formats = NULL,
  .labels = NULL,
  .indent_mods = NULL
)
}
\arguments{
\item{df}{(\code{logical} or \code{data.frame})\cr if only a logical vector is used,
it indicates whether each subject is a responder or not. \code{TRUE} represents
a successful outcome. If a \code{data.frame} is provided, the logical vector of
responses must be indicated as a variable name in \code{.var}.}

\item{.var}{(\code{string})}

\item{conf_level}{(\code{numeric})}

\item{long}{(\code{flag})\cr whether a long description is required.}

\item{na.rm}{(\code{flag})\cr whether \code{NA} responses should be removed before analysis.
If \code{FALSE} and \code{NA} values are present, an error is raised.}

\item{num_limit}{(\code{int})\cr numerator limit to trigger the exact method.}

\item{denom_limit}{(\code{int})\cr denominator limit to trigger the exact method.}

\item{denom}{(\code{character})\cr denominator to use for percentage and CI computation:
\code{"n"} (default, number of observed records), \code{"N_col"}, or \code{"N_row"}. When \code{"N_col"} or
\code{"N_row"} are chosen, the corresponding \code{.N_col} or \code{.N_row} are used, respectively.}

\item{.N_row}{(\code{int})}

\item{.N_col}{(\code{int})}

\item{...}{additional arguments for the lower level functions.}

\item{.stats}{(\code{character})\cr statistics to select for the table.}

\item{.formats}{(named \code{character} or \code{list})\cr formats for the statistics. See Details in \code{analyze_vars} for more
information on the \code{'auto'} setting.}

\item{.labels}{(named \code{character})\cr labels for the statistics (without indent).}

\item{.indent_mods}{(named \code{integer})\cr indent modifiers for the labels. Defaults to 0, which corresponds to the
unmodified default behavior. Can be negative.}
}
\value{
\itemize{
\item \code{s_cond_proportion_j()} returns statistics \code{n_prop} (\code{n} responders and proportion)
and \code{prop_ci} (proportion CI), formatted consistently with \code{\link[tern:s_proportion]{tern::s_proportion()}}.
}

\itemize{
\item \code{d_cond_proportion_j()} returns a string describing the analysis label.
}

\itemize{
\item \code{a_cond_proportion_j()} returns the corresponding list with formatted \code{\link[rtables:CellValue]{rtables::CellValue()}}.
}
}
\description{
The analysis function \code{\link[=a_cond_proportion_j]{a_cond_proportion_j()}} is used to create a layout
element that estimates a response proportion with confidence interval,
automatically selecting between exact and Wald methods based on observed
counts and user-defined limits.
}
\details{
The statistics function mirrors \code{\link[tern:s_proportion]{tern::s_proportion()}} usage and output but
removes the \code{method} argument and decides internally between
"clopper-pearson" and "wald".

The exact method is used when any of these are true:
(a) the number of responders is \code{num_limit} (or less),
(b) all subjects except \code{num_limit} (or less) have observed response,
or (c) the observed group size is less than \code{denom_limit}.

CI computation follows \code{\link[tern:s_proportion]{tern::s_proportion()}} conventions: helper functions
are called with \code{n = denom}, so when \code{denom = "N_col"} or \code{"N_row"}, those
denominators are used for the interval.
}
\section{Functions}{
\itemize{
\item \code{s_cond_proportion_j()}: Statistics function estimating a proportion
along with its confidence interval, with adaptive method selection.

\item \code{d_cond_proportion_j()}: Description helper function for the conditional
proportion summary label.

\item \code{a_cond_proportion_j()}: Formatted analysis function which is used as
\code{afun} for conditional proportion with adaptive CI selection (exact vs. Wald).

}}
\examples{
# Logical vector input
rsp_v <- c(TRUE, FALSE, TRUE, TRUE, FALSE, TRUE, FALSE, FALSE)
s_cond_proportion_j(rsp_v)

# Data frame input
dta <- data.frame(rsp = c(TRUE, TRUE, FALSE, TRUE, FALSE, NA))
s_cond_proportion_j(dta, .var = "rsp")

# Keep NAs and error if any are present
\dontrun{
s_cond_proportion_j(dta, .var = "rsp", na.rm = FALSE)
}

# Using different denominator (requires .N_col in ...)
s_cond_proportion_j(dta, .var = "rsp", denom = "N_col", .N_col = 10)

nex <- 100
dta <- data.frame(
  "rsp" = sample(c(TRUE, FALSE), nex, TRUE),
  "grp" = sample(c("A", "B"), nex, TRUE),
  "f1"  = sample(c("a1", "a2"), nex, TRUE),
  stringsAsFactors = TRUE
)

l <- basic_table() |>
  split_cols_by(var = "grp") |>
  analyze(
    vars = "rsp",
    afun = a_cond_proportion_j,
    extra_args = list(
      conf_level = 0.90,
      num_limit = 0,
      denom_limit = 10
    )
  )

build_table(l, df = dta)
}
