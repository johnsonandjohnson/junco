% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/s_diff_mean_ci.r
\name{s_diff_mean_ci}
\alias{s_diff_mean_ci}
\title{Difference in Means with Confidence Interval}
\usage{
s_diff_mean_ci(
  df1,
  df2,
  .var,
  paired = FALSE,
  paired_by = NULL,
  conf.level = 0.95,
  ...
)
}
\arguments{
\item{df1}{(\code{data.frame})\cr Dataset for the first sample.}

\item{df2}{(\code{data.frame})\cr Dataset for the second sample.}

\item{.var}{(\code{character(1)})\cr Column name in \code{df1} and \code{df2} containing
numeric values.}

\item{paired}{(\code{logical(1)})\cr Whether the samples are paired.}

\item{paired_by}{(\code{character} or \code{NULL})\cr Column name(s) in \code{df1} and \code{df2}
used to match observations between datasets. Required when \code{paired = TRUE}
and must uniquely identify each pair in both datasets.}

\item{conf.level}{(\code{proportion})\cr Confidence level for the interval.}

\item{...}{Additional arguments passed to \code{\link[=safe_t_test]{safe_t_test()}}.}
}
\value{
A named \code{list} with a single element \code{diff_mean_ci}, containing the difference
in means and confidence interval estimates.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Computes the difference in means between two samples along with a confidence
interval. The interval is computed using a t-distribution framework via
\code{\link[=safe_t_test]{safe_t_test()}}.

Supports both independent and paired samples. For paired data, observations
are matched using \code{paired_by}, and the inference is based on within-pair
differences using a paired t-distribution framework.
}
\details{
The first sample is taken from \code{df1[[.var]]} and the second from \code{df2[[.var]]}.

If \code{paired = TRUE}, observations are matched using \code{paired_by}. In this case,
the difference in means and its confidence interval are computed using a
t-statistic for paired data (based on within-pair differences). Otherwise,
a t-statistic for two independent samples is used.

Any \code{NA} or \code{NaN} values in columns specified by \code{paired_by} are ignored and
excluded from matching (see \code{merge(..., incomparables = c(NA, NaN))}).

When \code{paired = TRUE}, only complete pairs are passed to \code{\link[=safe_t_test]{safe_t_test()}}
(i.e., rows with missing values in \code{.var} are removed prior to computation).
For unpaired cases, missing values are removed separately from each sample
before computation.
}
\examples{
df1 <- data.frame(
  USUBJID = c("X01", "X02", "X03", "X04", "X05"),
  CHG = c(4, 1, -1, 9, -2)
)
df2 <- data.frame(
  USUBJID = c("X01", "X02", "X03", "X04", "X05"),
  CHG = c(-2, 4, -2, 5, 2)
)

# Paired
s_diff_mean_ci(df1, df2, "CHG", paired = TRUE, paired_by = "USUBJID")

# Unpaired
s_diff_mean_ci(df1, df2, "CHG")

}
