% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_sum_ratio_j.R
\name{a_sum_ratio_j}
\alias{a_sum_ratio_j}
\title{Analysis Function: Sum and Ratio of Sums}
\usage{
a_sum_ratio_j(
  df,
  .var,
  .stats = "sum",
  denom_by = NULL,
  id_var = NULL,
  .formats = NULL,
  .labels = NULL,
  .indent_mods = NULL,
  na_str = NULL,
  ...
)
}
\arguments{
\item{df}{(\code{data.frame})\cr data set containing all analysis variables.}

\item{.var}{(\code{string})\cr single variable name that is passed by \code{rtables} when requested
by a statistics function.}

\item{.stats}{(\code{character})\cr one or more of \code{"sum"}, \code{"sum_unique"},
\code{"ratio"}, \code{"ratio_unique"}. Default: \code{"sum"}.}

\item{denom_by}{(\code{string} or \code{NULL})\cr denominator column; required for
\code{"ratio"} and \code{"ratio_unique"}.}

\item{id_var}{(\code{string} or \code{NULL})\cr subject-id column for deduplication;
required for \code{"sum_unique"} and \code{"ratio_unique"}.}

\item{.formats}{(named \code{character} or \code{list})\cr formats for the statistics. See Details in \code{analyze_vars} for more
information on the \code{'auto'} setting.}

\item{.labels}{(named \code{character})\cr labels for the statistics (without indent).}

\item{.indent_mods}{(named \code{integer} or \code{NULL})\cr indent modifiers for the
row labels. Defaults to \code{NULL} (no modification).}

\item{na_str}{(\code{character} or \code{NULL})\cr string(s) used to replace \code{NA}
values in the formatted output. Defaults to \code{NULL} (no replacement).}

\item{...}{additional arguments for the lower level functions.}
}
\value{
A \code{RowsVerticalSection} for use by rtables.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Computes one or more of the following statistics for use with
\code{\link[rtables:analyze]{rtables::analyze()}}:
\itemize{
\item \code{"sum"}: \code{sum(.var)}
\item \code{"sum_unique"}: \code{sum(.var)} after deduplicating rows by \code{id_var}
\item \code{"ratio"}: \code{sum(.var) / sum(denom_by)}
\item \code{"ratio_unique"}: same ratio after deduplicating rows by \code{id_var}
}

Both \code{.var} and \code{denom_by} columns must be numeric (integer, double, or
logical coerced to numeric).
}
\examples{
df <- data.frame(
  USUBJID = c("S01", "S01", "S02", "S03"),
  ARM     = factor(c("A", "A", "A", "B")),
  EVENTS  = c(1, 1, 0, 1),
  DAYS    = c(10, 10, 20, 15)
)

# All 4 stats in a layout (S01 duplicate tests unique logic)
lyt <- basic_table() |>
  split_cols_by("ARM") |>
  analyze(
    "EVENTS",
    afun = a_sum_ratio_j,
    extra_args = list(
      .stats   = c("sum", "sum_unique", "ratio", "ratio_unique"),
      denom_by = "DAYS",
      id_var   = "USUBJID"
    )
  )

build_table(lyt, df)
}
\author{
VR
}
