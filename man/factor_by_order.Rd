% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{factor_by_order}
\alias{factor_by_order}
\title{Create a factor with levels ordered by a separate ordering vector}
\usage{
factor_by_order(x, y, ordered = FALSE)
}
\arguments{
\item{x}{(\code{character} or \code{factor})\cr A vector to be converted to a factor.}

\item{y}{(\code{integerish})\cr A vector defining the order of levels in \code{x}.
Must have the same length as \code{x}.}

\item{ordered}{(\code{logical(1)})\cr Indicates whether the result should be
an ordered factor. Defaults to \code{FALSE}.}
}
\value{
A factor created from \code{x}, with levels ordered according to \code{y}.
Attributes of \code{x} other than \code{class} and \code{levels} are preserved.
If \code{ordered = TRUE}, an ordered factor is returned.
If \code{x} is empty or consists entirely of missing values, a factor with
no levels is returned.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Converts a character vector or factor into a factor where the level order is
determined by a second integer-like vector containing the corresponding order
values.
}
\details{
The values in \code{x} and \code{y} must define a bijection between the unique
non-missing values: each unique value in \code{x} must correspond to exactly one
unique value in \code{y}, and vice versa.
Missing values are handled separately: \code{NA} values in \code{x} and \code{y} must
occur at the same positions. Missing values are not included as factor
levels.

If \code{x} is already a factor containing unobserved (unused) levels, these
unobserved levels will be dropped in the resulting factor.
}
\examples{
factor_by_order(c("A", "A", "B"), c(1, 1, 2))

factor_by_order(c("A", "A", "B"), c(1, 1, 2), ordered = TRUE)

factor_by_order(c("A", "A", "B"), c(2, 2, 1))

factor_by_order(c("A", "A", "B", NA), c(1, 1, 2, NA))

\dontrun{
factor_by_order(c("A", "A", "B", NA), c(1, 2, 2, 4))
# Error: `x` and `y` must define a bijection between their unique non-NA values; NA values must correspond.
}

}
\author{
WW
}
