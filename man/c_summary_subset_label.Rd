% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_summary.r
\name{c_summary_subset_label}
\alias{c_summary_subset_label}
\title{Summary Statistics for Filtered Data with Label}
\usage{
c_summary_subset_label(
  df,
  labelstr,
  .var,
  .spl_context,
  subset_expr,
  label,
  label_indent_mod = 0L,
  ...
)
}
\arguments{
\item{df}{(\code{data.frame})\cr data set containing all analysis variables.}

\item{labelstr}{(\code{character})\cr label of the level of the parent split currently being summarized
(must be present as second argument in Content Row Functions). See \code{\link[rtables:summarize_row_groups]{rtables::summarize_row_groups()}}
for more information.}

\item{.var}{(\code{string})\cr single variable name that is passed by \code{rtables} when requested
by a statistics function.}

\item{.spl_context}{(\code{data.frame})\cr gives information about ancestor split states
that is passed by \code{rtables}.}

\item{subset_expr}{(\code{expression} or \code{NULL})\cr
Logical expression used to subset rows of \code{df} before analysis.
Evaluated in the context of \code{df}.
Defaults to \code{expression(rep(TRUE, nrow(df)))}, meaning no filtering.}

\item{label}{(\code{string} or \code{function})\cr A label to be added to the output.
If a function is provided, it must accept a single argument \code{.spl_context}
and return a character string.}

\item{label_indent_mod}{(\code{integer(1)})\cr Indentation level applied to the
label row.}

\item{...}{Additional arguments passed to \code{\link[=a_summary_j]{a_summary_j()}}.}
}
\value{
An object returned by \code{\link[=a_summary_j]{a_summary_j()}} with an additional label applied.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A wrapper around \code{\link[=a_summary_j]{a_summary_j()}} that filters the data prior to execution
and prepends a label to the resulting summary statistics object.
}
\details{
The function first applies row filtering via \code{\link[=filter_df_prior_afun]{filter_df_prior_afun()}},
then computes summary statistics using \code{\link[=a_summary_j]{a_summary_j()}}, and finally
attaches a label to the resulting object.
}
\examples{
df <- data.frame(
  USUBJID = rep(1:6, each = 2),
  AVISIT = rep(c("Baseline", "Day 1"), 6),
  AVAL = c(1, 3, 2, 9, 13, 19, 15, 23, 43, 56, 24, 32),
  ABLFL = rep(c(TRUE, FALSE), 6),
  BASE = rep(c(1, 2, 13, 15, 43, 24), each = 2),
  CHG = c(0, 2, 0, 7, 0, 6, 0, 8, 0, 13, 0, 8)
)
df

c_summary_subset_label(
  df = df,
  .var = "CHG",
  subset_expr = expression(ABLFL),
  label = "Change from Baseline",
  .stats = c("n", "mean_sd")
)
}
