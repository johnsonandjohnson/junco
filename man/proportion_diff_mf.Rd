% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cond_proportion_diff.R
\name{proportion_diff_mf}
\alias{proportion_diff_mf}
\alias{s_proportion_diff_mf}
\alias{a_proportion_diff_mf}
\title{Proportion Difference with Method Selection Based on the Mantel-Fleiss
Criterion}
\usage{
s_proportion_diff_mf(
  df,
  .var = NULL,
  .in_ref_col = NULL,
  .ref_group = NULL,
  val = TRUE,
  variables = list(strata = NULL),
  na.rm = FALSE,
  conf_level = 0.95,
  mf_method = c("cmh", "cmh_sato", "cmh_mn")
)

a_proportion_diff_mf(
  df,
  .var,
  .in_ref_col = NULL,
  .ref_group = NULL,
  val = TRUE,
  variables = list(strata = NULL),
  na.rm = FALSE,
  conf_level = 0.95,
  mf_method = c("cmh", "cmh_sato", "cmh_mn"),
  ...,
  .stats = NULL,
  .formats = NULL,
  .labels = NULL,
  .indent_mods = NULL,
  exact_footnote = rtables:::RefFootnote(tern::d_proportion_diff(method =
    "uncond_exact_diff", method_only = TRUE), 1L, "+")
)
}
\arguments{
\item{df}{(\code{data.frame}) \cr
The data containing all analysis variables for the non-reference group.}

\item{.var}{(\code{character(1)}) \cr
The column name in \code{df} (and, if supplied, \code{.ref_group}) specifying the
response variable. The response is converted to a logical vector by
comparing its values with \code{val}. \code{df[[var]]} (and \code{.ref_group[[var]]}, if
supplied) must be an atomic vector as defined by
\code{checkmate::check_atomic_vector()}, of one of the following types:
\code{logical}, \code{integer}, \code{numeric}, or \code{character}.}

\item{.in_ref_col}{(\code{logical(1)} or \code{NULL}) \cr
Indicates whether the function is being evaluated for the reference
column. Set to \code{TRUE} for the reference column and to \code{FALSE} or \code{NULL}
otherwise.
For the statistics function, \code{TRUE} or \code{NULL} results in empty statistics,
while for the analysis function, the result is a \code{RowsVerticalSection} with
a \code{NULL} value for all selected statistics.}

\item{.ref_group}{(\code{data.frame} or \code{NULL}) \cr
The data corresponding to the reference group.}

\item{val}{(\code{logical(1)} or \code{integer(1)} or \code{numeric(1)} or \code{character(1)}) \cr
The value used to identify the response of interest. An observation is
considered a response of interest when \code{df[[.var]] == val} (and, if the
the reference group is supplied, \code{.ref_group[[.var]] == val}).
If \code{df[[.var]]} is a \code{factor}, \code{val} must be a \code{character} value matching
one of its levels. Otherwise, \code{val} must have the same class as \code{df[[.var]]}.}

\item{variables}{(\code{list} or \code{NULL}) \cr
Optional strata variables specified as \verb{list(strata = <strata_vars>)},
where \verb{<strata_vars>} is a character vector containing column names in
\code{df} (and, if supplied, \code{.ref_group}). All specified strata variables
must be factors.}

\item{na.rm}{(\code{logical(1)}) \cr
Whether to remove incomplete rows from \code{df} (and, if supplied, \code{.ref_group})
before constructing the response, group, and strata vectors.
Completeness is assessed only for \code{.var} and the strata columns (if supplied).
If \code{na.rm = TRUE}, rows containing missing values in any of these columns
are removed. If \code{na.rm = FALSE}, the function fails if any of these columns
contain missing values, rather than returning results containing \code{NA}
values.

This argument is passed unchanged to the \code{complete_cases} argument of
\code{\link[tern:h_prepare_rsp_table]{tern::h_prepare_rsp_table()}}.}

\item{conf_level}{(\code{proportion})\cr confidence level of the interval.}

\item{mf_method}{(\code{character(1)}) \cr
The method used for estimating the difference in response proportions and
its confidence interval when strata are provided and the Mantel-Fleiss
criterion is satisfied. Available choices are: \code{"cmh"}, \code{"cmh_sato"}, and
\code{"cmh_mn"}. See \strong{Details} for more information.}

\item{...}{additional arguments for the lower level functions.}

\item{.stats}{(\code{character}) \cr
Statistics to select. Available choices are \code{"diff"}, \code{"diff_ci"}, and
\code{"diff_est_ci"}. This parameter affects only the formatted analysis
function and has no effect on the statistics function.}

\item{.formats}{(named \code{character} or \code{list}) \cr
Formats for the statistics.}

\item{.labels}{(named \code{character})\cr labels for the statistics (without indent).}

\item{.indent_mods}{(named \code{integer})\cr indent modifiers for the labels. Defaults to 0, which corresponds to the
unmodified default behavior. Can be negative.}

\item{exact_footnote}{(\code{character(1)} or \code{RefFootnote}) \cr
Footnote used to indicate that the unconditional exact method was actually
used for the analysis.}
}
\value{
\itemize{
\item \code{s_proportion_diff_mf()} returns a list containing:
\describe{
\item{\code{diff} (\code{numeric(1)} or \code{numeric(0)})}{Point estimate of the
difference in response proportions, or \code{numeric(0)} when the function is
evaluated for the reference column (\code{.in_ref_col} is NULL or \code{.in_ref_col}
is \code{TRUE}).}
\item{\code{diff_ci} (\code{numeric(2)} or \code{numeric(0)})}{Confidence interval for
the difference in response proportions, or \code{numeric(0)} when the function
is evaluated for the reference column (\code{.in_ref_col} is NULL or
\code{.in_ref_col} is \code{TRUE}).}
\item{\code{diff_est_ci} (\code{numeric(3)} or \code{numeric(0)})}{Point estimate and
confidence interval for the difference in response proportions, or
\code{numeric(0)} when the function is evaluated for the reference column
(\code{.in_ref_col} is NULL or \code{.in_ref_col} is \code{TRUE}).}
\item{\code{executed_method} (\code{character(1)})}{Name of the method actually used
for the analysis. Takes the value \code{NA_character_} when the function is
evaluated for the reference column (\code{.in_ref_col} is \code{NULL} or
\code{.in_ref_col} is \code{TRUE}). Otherwise, identifies the method used:
\code{"cmh"}, \code{"cmh_sato"}, \code{"cmh_mn"}, or \code{"uncond_exact_diff"}.
Formatted analysis functions can use this value to determine whether an
appropriate footnote should be added.}
}
}

\itemize{
\item \code{a_proportion_diff_mf()} returns the corresponding \code{RowsVerticalSection}
object with formatted results.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Performs an adaptive analysis of the difference in response proportions
between a non-reference group and a reference group for stratified data, with
the method selected based on the Mantel-Fleiss (MF) criterion.

When the Mantel-Fleiss criterion is satisfied, a Cochran-Mantel-Haenszel
(CMH) analysis is performed using the method specified by \code{mf_method}.
When the criterion is not satisfied, an unconditional exact method is used
instead.
}
\details{
The data are prepared by \code{\link[tern:h_prepare_rsp_table]{tern::h_prepare_rsp_table()}}, which constructs the
response vector, group indicator, optional strata variable, and the
corresponding 2 x 2 contingency table(s). When multiple strata variables are
supplied, the cross-combinations of their levels define the analysis strata.

The response variable is converted to a binary response according to \code{val}.
The analysis compares the response proportion in the non-reference group with
that in the reference group. The difference is calculated as the
non-reference group minus the reference group.

When strata are provided and the Mantel-Fleiss criterion is satisfied, the
selected CMH method \code{\link[tern:prop_diff_cmh]{tern::prop_diff_cmh()}} is used to obtain the point
estimate and confidence interval for the difference in response proportions.
The available Cochran-Mantel-Haenszel-type methods for estimating the
difference in response proportions and its confidence interval are:
\itemize{
\item \code{"cmh"}: CMH method.
\item \code{"cmh_sato"}: CMH method using the Sato variance estimator.
\item \code{"cmh_mn"}: CMH method using the Miettinen-Nurminen method.
}

See \code{\link[tern:prop_diff_cmh]{tern::prop_diff_cmh()}} for more details and references.

When strata are not specified, or when strata are specified but the
Mantel-Fleiss criterion is not satisfied or cannot be evaluated (e.g., when
all strata contain no observed values), the unconditional exact method
\code{\link[tern:prop_diff_uncond_exact]{tern::prop_diff_uncond_exact()}} is used to obtain the point estimate and
confidence interval.

The Mantel-Fleiss criterion is evaluated using \code{\link[tern:mantel_fleiss_crit]{tern::mantel_fleiss_crit()}}.

The statistics function \code{s_proportion_diff_mf()} follows the usage and output
of \code{\link[tern:s_proportion_diff]{tern::s_proportion_diff()}} but removes the \code{method} argument and selects
internally between a Cochran-Mantel-Haenszel-type method and an unconditional
exact method based on the Mantel-Fleiss criterion.
}
\section{Functions}{
\itemize{
\item \code{s_proportion_diff_mf()}: Statistics function estimating the difference
in response proportions and its confidence interval.
When strata variables are provided and the Mantel-Fleiss criterion is
satisfied, the CMH method \code{\link[tern:prop_diff_cmh]{tern::prop_diff_cmh()}} is used, with the
\code{diff_se} argument determined by \code{mf_method}. Otherwise, i.e., when strata
variables are not provided, or when strata variables are provided but the
Mantel-Fleiss criterion is not satisfied or cannot be evaluated,
the unconditional exact method \code{\link[tern:prop_diff_uncond_exact]{tern::prop_diff_uncond_exact()}} is used.

\item \code{a_proportion_diff_mf()}: Formatted analysis function used as \code{afun}
for the difference in response proportions with method selection based on
the Mantel-Fleiss criterion.

}}
\examples{
n <- 100 # Number of observations.
set.seed(123)
dta <- data.frame(
  "rsp" = sample(c("Y", "N"), n, TRUE),
  "grp" = c(
    sample(c("A", "Placebo"), round(n - 0.1 * n), TRUE),
    rep("C", round(0.1 * n))
  ),
  "f1" = sample(c("a1", "a2"), n, TRUE),
  "f2" = sample(c("x", "y", "z"), n, TRUE),
  stringsAsFactors = TRUE
)

prop_d <- s_proportion_diff_mf(
  df = subset(dta, grp == "A"),
  .var = "rsp",
  .ref_group = subset(dta, grp == "Placebo"),
  .in_ref_col = FALSE,
  val = "Y",
  variables = list(strata = c("f1", "f2")),
  conf_level = 0.90,
  mf_method = "cmh_mn"
)
prop_d

lyt <- basic_table() |>
  split_cols_by(var = "grp", ref_group = "Placebo") |>
  analyze(
    vars = "rsp",
    afun = a_proportion_diff_mf,
    extra_args = list(
      val = "Y",
      variables = list(strata = c("f1", "f2")),
      conf_level = 0.90,
      mf_method = "cmh_mn",
      .stats = "diff_est_ci"
    )
  )

build_table(lyt, df = dta)

}
\seealso{
\code{\link[=cond_proportion_j]{cond_proportion_j()}}
}
