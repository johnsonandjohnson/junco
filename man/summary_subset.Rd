% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_summary_subset.r
\name{summary_subset}
\alias{summary_subset}
\alias{a_summary_subset}
\alias{c_summary_subset_label}
\title{Wrappers Around \code{tern::a_summary()} with Optional Subsetting and Labels}
\usage{
a_summary_subset(df, .var, filter_expr, ...)

c_summary_subset_label(
  df,
  labelstr,
  .var,
  filter_expr,
  label,
  label_indent_mod = 0L,
  ...
)
}
\arguments{
\item{df}{(\code{data.frame})\cr data set containing all analysis variables.}

\item{.var}{(\code{string})\cr single variable name that is passed by \code{rtables} when requested
by a statistics function.}

\item{filter_expr}{(\code{expression})
A single logical expression wrapped in \code{expression(...)} used to subset
rows of \code{df} before summarization.
The expression is evaluated using \code{\link[=eval]{eval()}} in an environment constructed
from \code{df}, meaning that all column names are available as variables.
Exactly one expression is allowed.}

\item{...}{Additional arguments passed to \code{\link[tern:analyze_variables]{tern::a_summary()}}, excluding
the \code{x} argument, which is constructed internally from \code{df[[.var]]}
after applying the filter.}

\item{labelstr}{(\code{character})\cr label of the level of the parent split currently being summarized
(must be present as second argument in Content Row Functions). See \code{\link[rtables:summarize_row_groups]{rtables::summarize_row_groups()}}
for more information.}

\item{label}{(\code{character(1)})\cr Label to be inserted as the first row.}

\item{label_indent_mod}{(\code{integer(1)})\cr Indentation level applied to the
label row.}
}
\value{
Returns the same type of object as \code{\link[tern:analyze_variables]{tern::a_summary()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

These wrappers around \code{\link[tern:analyze_variables]{tern::a_summary()}} allow optional row subsetting
using a logical expression evaluated in the context of the input data frame.

\code{a_summary_subset()} is an \link[rtables:rtables-package]{rtables::rtables} analysis function (see
\code{\link[rtables:analyze]{rtables::analyze()}}), while \code{c_summary_subset_label()} is a content row
function (see \code{\link[rtables:summarize_row_groups]{rtables::summarize_row_groups()}}) that may optionally
append a label to the resulting content row.
}
\details{
\code{\link[tern:analyze_variables]{tern::a_summary()}} is a formatted analysis function that computes summary
statistics for a single variable. In typical use within the \link[rtables:rtables-package]{rtables::rtables}
framework, the analysis variable is first extracted from a data frame and
then passed to \code{\link[tern:analyze_variables]{tern::a_summary()}} as a standalone vector.

The \code{a_summary_subset()} and \code{c_summary_subset_label()} wrappers extend
this behavior by:
\itemize{
\item accepting a data frame (\code{df}) as input,
\item optionally subsetting rows using a logical expression,
\item extracting the analysis variable \code{.var} after subsetting,
\item and passing the resulting vector to \code{\link[tern:analyze_variables]{tern::a_summary()}}.
}
}
\section{Functions}{
\itemize{
\item \code{a_summary_subset()}: Analysis function with optional subsetting.

\item \code{c_summary_subset_label()}: Content row function with optional subsetting
and an optional label prepended to the resulting content row using
\code{\link[=prepend_label_cell]{prepend_label_cell()}}.

}}
\examples{

## Analysis function:

df <- data.frame(
  ID = 1:8,
  AGE = c(34, NA, 52, 45, 50, NA, 41, 29),
  AVAL = c(1.2, 3, -1.5, 0.8, 3.5, 2, -1.1, 0.3)
)
df

stats <- c("n", "sum")

tern::a_summary(df$AVAL, .stats = stats)

a_summary_subset(df, "AVAL", expression(!is.na(AGE)), .stats = stats)
min_age <- 45
a_summary_subset(df, "AVAL", expression(AGE > min_age), .stats = stats)
a_summary_subset(df, "AVAL", expression(AGE > 30 & AVAL > 0), .stats = stats)


## Content row function:

df <- data.frame(
  USUBJID = rep(1:6, each = 2),
  AVISIT = rep(c("Baseline", "Day 1"), 6),
  AVAL = c(1, 3, 2, 9, 13, 19, 15, 23, 43, 56, 24, 32),
  ABLFL = rep(c(TRUE, FALSE), 6),
  BASE = rep(c(1, 2, 13, 15, 43, 24), each = 2),
  CHG = c(0, 2, 0, 7, 0, 6, 0, 8, 0, 13, 0, 8)
)
df

c_summary_subset_label(
  df = df,
  .var = "BASE",
  filter_expr = expression(ABLFL),
  label = "Baseline",
  .stats = c("n", "mean_sd")
)
}
\seealso{
\code{\link[tern:analyze_variables]{tern::a_summary()}}
}
