% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/insert_line.R
\name{insert_line}
\alias{insert_line}
\title{Insert a Single Line into a Layout}
\usage{
insert_line(
  lyt,
  afun = ac_blank_line,
  table_names = NULL,
  indent_mod = 0L,
  ...
)
}
\arguments{
\item{lyt}{(\code{layout})\cr input layout where analyses will be added to.}

\item{afun}{(\code{function}) \code{rtables} analysis function that produces exactly one
row of output (for example, a single \code{CellValue}). Defaults to \code{ac_blank_line}
for inserting a blank or static labeled line.}

\item{table_names}{(\code{character})\cr this can be customized in case that the same \code{vars} are analyzed multiple times,
to avoid warnings from \code{rtables}.}

\item{indent_mod}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Please use the \code{.indent_mods} argument instead.}

\item{...}{Additional arguments passed to \code{afun} via
\code{analyze(extra_args = list(...))}.}
}
\value{
A modified \code{PreDataTableLayouts} object containing the inserted line
after the current row content.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Inserts a single-row "gap" or labeled line into an \code{rtables} layout by
appending an additional \code{\link[rtables:analyze]{rtables::analyze()}} step for the most recently used
row-analysis variable. This is useful for adding visual separators or
headings between blocks of rows.
}
\details{
This helper appends another \code{\link[rtables:analyze]{rtables::analyze()}} call targeting the last
variable present in the row layout (as returned by \code{\link[rtables:vil]{rtables::vars_in_layout()}}),
so it should be called after at least one prior analysis step has introduced
a row-analysis variable.

If \code{table_names} is not provided, the function generates a unique internal
table name for the injected line. To avoid duplicate table names across
multiple invocations, it maintains a simple counter in the R option
\code{"junco.insert_line"}. This stateful approach:
\itemize{
\item is not thread-safe and may not work with parallelisation across multiple
threads/processes, and
\item can make the resulting \code{rtables} object non-reproducible unless the option
is controlled/reset between runs.
}

For reproducible or parallel workflows, pass an explicit, globally unique
\code{table_names} value.
}
\examples{
afun_N <- function(x) rcell(length(x), label = "N")
afun_mean <- function(x) rcell(mean(x), label = "Mean")

# Blank line example
lyt <- basic_table() |>
  split_rows_by("STRATA1") |>
  analyze(vars = "AGE", afun = afun_N) |>
  insert_line() |>
  analyze(vars = "AGE", afun = afun_mean, table_names = "AGE_mean")

build_table(lyt, ex_adsl)

# Labelled line example
lyt2 <- basic_table() |>
  split_rows_by("STRATA1") |>
  analyze(vars = "AGE", afun = afun_N) |>
  insert_line(labelstr = "Point Estimates:") |>
  analyze(vars = "AGE", afun = afun_mean, table_names = "AGE_mean")

build_table(lyt2, ex_adsl)

}
\seealso{
\code{\link[rtables:analyze]{rtables::analyze()}}, \code{\link[rtables:vil]{rtables::vars_in_layout()}}
}
