% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_df_prior_afun.r
\name{filter_df_prior_afun}
\alias{filter_df_prior_afun}
\title{Filter Data Prior To Analysis Function}
\usage{
filter_df_prior_afun(
  df,
  .var,
  afun,
  subset_expr = expression(rep(TRUE, nrow(df))),
  ...
)
}
\arguments{
\item{df}{(\code{data.frame})\cr data set containing all analysis variables.}

\item{.var}{(\code{string})\cr single variable name that is passed by \code{rtables} when requested
by a statistics function.}

\item{afun}{(\code{function})\cr Analysis function. Must accept \code{x} or \code{df} as
its first parameter. Can optionally take other parameters.}

\item{subset_expr}{(\code{expression} or \code{NULL})\cr
Logical expression used to subset rows of \code{df} before analysis.
Evaluated in the context of \code{df}.
Defaults to \code{expression(rep(TRUE, nrow(df)))}, meaning no filtering.}

\item{...}{Additional arguments passed to \code{afun}.}
}
\value{
The object returned by \code{afun} applied to the filtered dataset.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Applies row filtering to a dataset before executing a user-supplied analysis
function.
}
\details{
This is a generic wrapper that:
\enumerate{
\item Subsets \code{df} using \code{subset_expr}.
\item Passes data to \code{afun}, depending on its first argument. If it is named:
\itemize{
\item \code{x}, then \code{df[[.var]]} is passed.
\item \code{df}, then the \code{df} data frame is passed.
}
\item Forwards \code{.var} (if it is present in the formal arguments of \code{afun})
and all additional arguments (\code{...}) to \code{afun}.
}
}
\examples{
df <- data.frame(
  USUBJID = rep(1:6, each = 2),
  AVISIT = rep(c("Baseline", "Day 1"), 6),
  AVAL = c(1, 3, 2, 9, 13, 19, 15, 23, 43, 56, 24, 32),
  ABLFL = rep(c(TRUE, FALSE), 6),
  BASE = rep(c(1, 2, 13, 15, 43, 24), each = 2),
  CHG = c(0, 2, 0, 7, 0, 6, 0, 8, 0, 13, 0, 8)
)
df

afun <- tern::a_summary
.stats <- c("n", "mean_sd")

# No filtering.
filter_df_prior_afun(df, "CHG", afun, .stats = .stats)

# Baseline records only.
filter_df_prior_afun(df, "CHG", afun, expression(ABLFL), .stats = .stats)
}
