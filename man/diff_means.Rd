% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_diff_means.R
\name{diff_means}
\alias{diff_means}
\alias{s_diff_means}
\alias{a_diff_means}
\title{Difference in Means}
\usage{
s_diff_means(df1, df2, .var, paired = FALSE, paired_by = NULL, ...)

a_diff_means(
  df,
  .var,
  .spl_context,
  ref_path = NULL,
  ...,
  .stats = NULL,
  .formats = NULL,
  .labels = NULL,
  .indent_mods = NULL
)
}
\arguments{
\item{df1}{(\code{data.frame})\cr Dataset for the first sample.}

\item{df2}{(\code{data.frame})\cr Dataset for the second sample.}

\item{.var}{(\code{character(1)})\cr Name of the column in the dataset(s)
containing observed sample values.}

\item{paired}{(\code{logical(1)})\cr Whether the samples are paired.}

\item{paired_by}{(\code{character} or \code{NULL})\cr Column name(s) in \code{df1} and \code{df2}
used to match observations between datasets.
Required when \code{paired = TRUE} and must uniquely identify each pair in both
datasets.}

\item{...}{Additional named arguments passed to \code{s_diff_means()} and
\code{safe_t_test()}.}

\item{df}{(\code{data.frame})\cr Dataset for the first sample.}

\item{.spl_context}{(\code{data.frame})\cr gives information about ancestor split states
that is passed by \code{rtables}.}

\item{ref_path}{(\code{character})\cr Global reference group specification; see
\code{\link[=get_ref_info]{get_ref_info()}}. This specifies the column path for the second sample.
Specifically, the second sample is \code{.ref_group[[.var]]}, where
\code{.ref_group} is computed by \code{\link[=get_ref_info]{get_ref_info()}} using \code{.spl_context} and
\code{ref_path}.

This is a workaround for an \link[rtables:rtables-package]{rtables::rtables} limitation in supporting
\code{.ref_group} / \code{.in_ref_col} in the presence of stacked headers.
Normally, \code{.ref_group} and \code{.in_ref_col} are provided by the \link[rtables:rtables-package]{rtables::rtables}
tabulation framework when the current column split is within the reference
variable.}

\item{.stats}{(\code{character})\cr Statistics to include in the table.

Available options are:
\verb{'diff_means_n1', 'diff_means_n2', 'diff_means_est', 'diff_means_se', 'diff_means_est_se', 'diff_means_ci', 'diff_means_est_ci'}

If \code{NULL}, all statistics are included.}

\item{.formats}{(named \code{character} or \code{list})\cr formats for the statistics. See Details in \code{analyze_vars} for more
information on the \code{'auto'} setting.}

\item{.labels}{(named \code{character})\cr labels for the statistics (without indent).}

\item{.indent_mods}{(named \code{integer})\cr indent modifiers for the labels. Defaults to 0, which corresponds to the
unmodified default behavior. Can be negative.}
}
\value{
\itemize{
\item \code{s_diff_means()} returns a named \code{list} containing the quantities described
in the Description section.
}

\itemize{
\item \code{a_diff_means()} returns formatted \code{\link[rtables:in_rows]{rtables::in_rows()}} result with the
quantities requested in \code{.stats}.
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Computes estimates and associated inferential statistics for the estimand
defined as the difference in population means between two distributions.

The following quantities are available:
\describe{
\item{diff_means_n1}{Sample size of Group 1.}
\item{diff_means_n2}{Sample size of Group 2.}
\item{diff_means_est}{Point estimate of the difference in population means.}
\item{diff_means_se}{Standard error of the estimator of the difference in population means.}
\item{diff_means_est_se}{The point estimate and the standard error.}
\item{diff_means_ci}{Confidence interval for the difference in population means.}
\item{diff_means_est_ci}{The point estimate and the confidence interval.}
}
}
\details{
Supports both non-paired and paired samples. For non-paired samples, the
estimator is defined as the difference between sample means. For paired
samples, the estimator is defined as the mean of within-pair differences.

Inferential statistics, including standard errors and confidence intervals,
are obtained using a t-distribution framework via the internal
\code{safe_t_test()} function.

It supports only numeric variables.
Only complete (non-missing) observations are used in statistical computations.
Non-finite values (i.e., \code{Inf}, \code{-Inf}) are not allowed.
}
\section{Functions}{
\itemize{
\item \code{s_diff_means()}: Statistical function that computes estimates of the
difference in means.
The first sample is taken from \code{df1[[.var]]} and the second from
\code{df2[[.var]]}.

If \code{paired = FALSE}, missing values are removed separately from each sample
prior to statistical computations.
If \code{paired = TRUE}, observations are matched using \code{paired_by} prior to
statistical analysis, and only complete pairs are used.

Data extraction, alignment, and missing-value handling are delegated to the
internal \code{extract_vectors()} function, which prepares cleaned numeric
vectors for both paired and unpaired settings.

Inferential statistics are then computed using \code{safe_t_test()} applied to
the processed vectors.

\item \code{a_diff_means()}: Formatted analysis function which is used as \code{afun} in
\code{\link[rtables:analyze]{rtables::analyze()}}.

The sample from the first distribution is \code{df[[.var]]}, and the sample from
the second distribution is \code{.ref_group[[.var]]}, where \code{.ref_group} is
computed by \code{\link[=get_ref_info]{get_ref_info()}} using \code{.spl_context} and \code{ref_path}.

If \code{ref_path = NULL}, then \code{a_diff_means()} returns an \code{\link[rtables:in_rows]{rtables::in_rows()}}
result with an empty \code{formatted_cell} for every statistic specified in
\code{.stats}.

}}
\examples{

## Statistics function:

df1 <- data.frame(id = c("A", "B", "C", "D"), value = 1:4)
df2 <- data.frame(id = c("A", "C", "D", "E", "F"), value = c(3:1, NA, 16))

# Unpaired samples.
s_diff_means(df1, df2, "value", conf.level = .8)

# Paired samples.
s_diff_means(df1, df2, "value", paired = TRUE, paired_by = "id")


## Analysis  function:

lyt <- basic_table() |>
  split_cols_by("ARM") |>
  split_rows_by(
    "PARAMCD",
    labels_var = "PARAM",
    split_fun = keep_split_levels(c("SYSBP", "DIABP"))
  ) |>
  split_rows_by(
    "AVISIT",
    split_fun = keep_split_levels(c("BASELINE", "WEEK 1 DAY 8"))
  ) |>
  analyze(
    "AVAL",
    afun = tern::a_summary,
    extra_args = list(.stats = c("n", "mean")),
    show_labels = "hidden"
  ) |>
  analyze(
    "AVAL",
    afun = a_diff_means,
    table_names = "aval_diff",
    extra_args = list(
      .stats = "diff_means_est_ci",
      ref_path = c("ARM", "B: Placebo")
    ),
    show_labels = "hidden"
  )

tbl <- build_table(lyt, formatters::ex_advs)
tbl

}
\seealso{
\code{safe_t_test()}, \code{extract_vectors()}
}
