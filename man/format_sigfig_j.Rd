% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jjcsformats.R
\name{format_sigfig_j}
\alias{format_sigfig_j}
\title{Format numeric values by significant figures.}
\usage{
format_sigfig_j(
  sigfig,
  format = "xx",
  whole_integer = TRUE,
  zero_threshold = 10^(-2 * sigfig),
  trail_zero = TRUE
)
}
\arguments{
\item{sigfig}{(\code{integer(1)})\cr number of significant figures to display.}

\item{format}{(\code{character(1)})\cr layout format string controlling how one or two
values are arranged, e.g. \code{"xx"}, \code{"xx (xx)"}, \code{"(xx, xx)"}. Any decimal
specification in \code{xx.} or \verb{xx.x+} is stripped — precision is controlled by
\code{sigfig} alone.}

\item{whole_integer}{(\code{logical(1)})\cr if \code{TRUE}, preserves the full integer part when
\code{x} has more integer digits than \code{sigfig} (i.e., scale is clamped to 1).}

\item{zero_threshold}{(\code{numeric(1)})\cr values with \code{abs(x) < zero_threshold} are snapped
to zero before rounding. Defaults to \code{0} (no snapping).}

\item{trail_zero}{(\code{logical(1)})\cr If \code{TRUE} flag code \code{"#"} passed to the \code{flag}
argument of \code{\link[=formatC]{formatC()}}. Defaults to \code{TRUE} (present trailing zeros).}

\item{round_type}{(\code{character(1)})\cr rounding method. See \code{\link[formatters:format_value]{formatters::format_value()}} for details.\cr
The resulting formatting function is of the type \verb{function(x, round_type, ...)},
which allows for different rounding types (see \code{\link[formatters:round_fmt]{formatters::round_fmt()}}).}
}
\value{
A formatting function with signature \verb{function(x, round_type, ...)} that
returns a formatted string.
}
\description{
Format numeric values by significant figures with controlable round_type,
whole_integer and trailing zeroes.
}
\details{
A function factory that produces formatting functions to round values to a
specified number of significant figures, suitable for use with
\code{\link[formatters:format_value]{formatters::format_value()}}.\cr
\cr The function is an alternative to \code{\link[tern:format_sigfig]{tern::format_sigfig()}},
Current function has fixed the \code{formatC} argument \code{num_fmt = "fg"},
and extra options for \code{whole_integer} and \code{trail_zero} to control the format behavior.
}
\examples{
fmt <- format_sigfig_j(3)
fmt(0.001234)
fmt(1234.5)

fmt2 <- format_sigfig_j(3, format = "xx (xx)")
fmt2(c(1234.5, 0.00567))


format_sigfig_j(3, trail_zero = TRUE)(0.01)
format_sigfig_j(3, trail_zero = FALSE)(0.01)
}
\seealso{
\code{\link[tern:format_sigfig]{tern::format_sigfig()}}
}
