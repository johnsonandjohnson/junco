# Assert that rbmi package is installed

Checks if the 'rbmi' package is installed and stops with an error
message if it's not.

## Usage

``` r
assert_rbmi()
```

## Value

Invisible NULL if rbmi is installed
